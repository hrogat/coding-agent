spring:
  application:
    name: coding-agent
  ai:
    mistralai:
      api-key: ${MISTRAL_API_KEY}
      chat:
        options:
          model: mistral-large-latest
          temperature: 0.7

server:
  port: 8080

logging:
  level:
    com.codingagent: DEBUG
    org.springframework.ai: DEBUG
